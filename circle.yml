dependencies:
  cache_directories:
    - jvamp-1.3
  pre:
    - mvn install:install-file -DgroupId=org.vamp_plugins -DartifactId=jvamp -Dversion=1.3 -Dpackaging=jar -Dfile=./lib/jvamp.jar
    - sudo apt-get install vamp-plugin-sdk
    - echo "export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64" >> ~/.bashrc && source ~/.bashrc
    - if [[ ! -e jvamp-1.3/libvamp-jni.so ]]; then wget https://code.soundsoftware.ac.uk/attachments/download/1574/jvamp-1.3.tar.gz && tar -xzf jvamp-1.3.tar.gz && cd jvamp-1.3 && make && cd ..; fi
    - cp jvamp-1.3/libvamp-jni.so /lib
