dependencies:
  pre:
    - mvn install:install-file -DgroupId=org.vamp_plugins -DartifactId=jvamp -Dversion=1.3 -Dpackaging=jar -Dfile=./lib/jvamp.jar
    - sudo cp ./lib/libvamp-jni.so ./lib/libvamp-hostsdk.so.3 ./lib/nnls-chroma.so /lib
    - sudo cp ./lib/libstdc++.so.6 /usr/lib/x86_64-linux-gnu/